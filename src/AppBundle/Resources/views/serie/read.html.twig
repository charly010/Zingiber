{% extends "@App/base.html.twig" %}

{% block body %}

{% include "@App/header.html.twig" %}
    <h1>Serie</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ serie.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ serie.title }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('serie_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('serie_edit', { 'id': serie.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>


    <h1>Reading...</h1>

    {#

    todo
    https://symfony.com/doc/master/bundles/EasyAdminBundle/integration/vichuploaderbundle.html

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sketch.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>
                <a href="{{ path('serie_show', { 'id': sketch.serie.id }) }}">{{ sketch.serie.name }}</a>
                </td>
            </tr>
            <tr>
                <th>Collection</th>
                <td>{{ sketch.serie }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ sketch.image }}</td>
            </tr>
            <tr>
                <th>Aperçu</th>
                <td><img src="{{app.request.basepath}}/uploads/sketchs/{{ sketch.image }}"/></td>
            </tr>
            
        </tbody>
    </table>

    #}

    {% include "@App/footer.html.twig" %}

{% endblock %}
