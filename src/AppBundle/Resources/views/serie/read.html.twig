{% extends "@App/base.html.twig" %}

{% block body %}

{% include "@App/header.html.twig" %}
    <h1>Serie</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ serie.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ serie.title }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('serie_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('serie_edit', { 'id': serie.id }) }}">Edit</a>
        </li>
    </ul>


    <h1>Reading...</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sketch.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ sketch.image }}</td>
            </tr>
            <tr>
                <th>Aperçu</th>
                <td>
                    <a href="{{ path('serie_read', { 'id': sketch.serie.id, 'page': nextPage }) }}">
                    <img src="{{app.request.basepath}}/uploads/sketchs/{{ sketch.image }}"/>
                    </a>
                </td>
            </tr>
        </tbody>
    </table>



    {% include "@App/footer.html.twig" %}

{% endblock %}
